{
  "name": "coliflow",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently -n CLIENT,SERVER -c blue,green \"npm run dev:client\" \"npm run dev:server\"",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && php artisan serve",
    "test": "concurrently -n FRONTEND,BACKEND -c magenta,cyan \"npm run test:client\" \"npm run test:server\"",
    "test:client": "cd client && npm run test",
    "test:server": "cd server && php artisan test"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
