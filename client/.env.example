# Base URL of the Laravel backend.
# Local development can leave this unset (it falls back to http://localhost:8000).
# In production (Vercel/Netlify), set it to your deployed backend, e.g.:
# VITE_API_URL=https://coliflow-api.up.railway.app
VITE_API_URL=http://localhost:8000
